<h1>{{ product.title }}</h1>
<img
  src="{{product.featured_image | image_url: width: 200, height:200 }}"
  width="200px"
  height="200px"
  alt="{{product.title}}"
>

{% comment %}
  how to fetch data on any section or browser
  fetch(`${location.pathname}/?section_id=test`)
    .then(res => res.text())
    .then(data => {
      const parser = new DOMParser();
      const database = parser.parseFromString(data, 'text/html');
      const newSection = database.getElementById('shopify-section-test');
      document.body.appendChild(newSection);
    });
{% endcomment %}
